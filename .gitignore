node_modules/
build/
dist/
coverage/
.DS_Store
*.log
.env*
!.env.example

# The project is npm-based (all scripts use npm). Remove the stale bun.lock and
# keep package-lock.json as the single source of truth for dependencies.
bun.lock
